
#include "source.cpp"

using namespace std;

int main()
{

    Person person;
    // /*
    strcpy(person.customer.CustomerName, "Ronke Ahmed");
    strcpy(person.customer.PhoneNumber, "08067701544");

    person.product.UnitPrice = 50;
    person.product.Quantity = 5;
    person.product.TotalPrice = 250;

    strcpy(person.shop.ShopName, "Yem Yem Ltd");
    strcpy(person.shop.Address, "University of Lagos, Akoka");

    //  */

    // FillShoppingInfo(person);
    WriteReceipt(person);
    return 0;
}
